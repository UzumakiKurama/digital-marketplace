/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : [
            "localhost",
            "digital-marketplace-production-547f.up.railway.app"
        ]
    }
};

export default nextConfig;
